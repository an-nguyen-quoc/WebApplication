@using Models;
@{
    ViewBag.Title = "Accounts";
    var view = new VstViewFormat
    {
        DataSource = Model,
        Columns = new List<ColumnInfo>
{
            new ColumnInfo { Name = "Id",  Caption = "Tên người dùng", AllowNull = false, Type="text" },
            new ColumnInfo { Name = "Role",  Caption = "Role", Input = "select"},
            //new ColumnInfo { Name = "Password",  Caption = "Mật khẩu", Input = "none" },
        },
    };
}

<h2>@ViewBag.Title - Test</h2>
@*<style>
    .Id {
        width: 200px;
        background-color: whitesmoke;
    }
    .Role {
        width: 80px;
    }
</style>*@

<div id="main-content"></div>

<script>
    var fields = @view.HtmlColumns;
    var values = @view.HtmlData;

    fields[1].options = ["USER", "MOD"];
    var table = new ApiTable("/account/", fields, values, 7, "gear");
</script>
